# Authors

**dev** — creator and maintainer.

GitHub: [devendrayadv](https://github.com/devendrayadv)

Project metadata, copyright, and documentation credit dev. Commits made through
GitHub's connector use the connected account identity, `devendrayadv`, and its
GitHub noreply address. The repository's `.mailmap` records `dev` as the canonical
name for mailmap-aware tools; it does not rewrite raw commit metadata.

When `scripts/publish.sh` initializes a new local repository, it configures
`dev` as the author name. Existing repositories retain their Git configuration.
Contributors retain their own attribution.
